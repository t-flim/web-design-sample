import React from "react"
import ReactDOM from "react-dom/client"
import App from "./App"

ReactDOM.createRoot(document.getElementById("i-am-root")).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
